# SmartShule — Configuration npm
# Garantit l'installation des devDependencies même avec NODE_ENV=production
# (nécessaire car @tailwindcss/postcss est requis pendant le build)
install-strategy=nested
legacy-peer-deps=true
